# =============================================================================
# guoqie_paths.cfg - 过切检查批处理路径配置（可直接编辑）
#
# 格式：键=值，# 开头为注释，空行忽略
# 相对路径相对于项目根目录（过切检查/）
# 首次使用前请在 VS 中编译 DLL（Release | x64 -> 生成解决方案）
# =============================================================================

INPUT_DIR=input
OUTPUT_DIR=output

# DLL 路径：可选，默认 scripts/guoqiejiancha.dll
# DLL_PATH=

NX_RUN_JOURNAL=C:\Program Files\Siemens\NX2312\NXBIN\run_journal.exe
DEFAULT_WORKERS=3
